
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agap_management
    username: root
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  properties:
    hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8080

application:
  security:
    jwt:
      secret-key: cNYIfqkRXwKcwvywkb0WkkfYX2l7OLLaSvs3ga6NcnY=
      expiration: 86400000  # a day
      refresh-token:
        expiration: 604800000   # 7 days

# Java
  #mail:
  #host: localhost
  #port: 1025
  #username: ali
  #password: ali
    #properties:
      #mail:
        #smtp:
        #trust: "*"
      #auth: true
        #starttls:
        #enabled: true
      #connectiontimeout: 5000
      #timeout: 3000
      #writetimeout: 5000


logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG

